using System;
using System.Linq;

public class 정수_내림차순으로_배치하기 {
    public long solution(long n) {
        string s = string.Concat(n.ToString().OrderByDescending(ch => ch));
        long answer = Convert.ToInt64(s);
        return answer;
    }
}