using System;

public class 두_정수_사이의_합 {
    public long solution(int a, int b) {
        long answer = 0;
        (int min, int max) = a > b ? (b, a) : (a, b);
        for(int i=min; i<=max; i++) answer += i;
        return answer;
    }
}